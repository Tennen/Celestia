{
  "name": "celestia-monorepo",
  "private": true,
  "workspaces": [
    "web/admin"
  ],
  "scripts": {
    "dev": "npm run dev --workspace web/admin",
    "build": "npm run build --workspace web/admin",
    "preview": "npm run preview --workspace web/admin",
    "build:web": "npm run build --workspace web/admin",
    "dev:web": "npm run dev --workspace web/admin"
  }
}
